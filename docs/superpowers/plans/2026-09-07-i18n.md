# Локализация KeenSwitch — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Вынести все пользовательские тексты расширения в каталоги `chrome.i18n` и поддержать шесть языков: en, ru, uk, pl, de, tr.

**Architecture:** Нативный `chrome.i18n`: каталоги лежат в `public/_locales/<locale>/messages.json`, Vite копирует их в корень `dist/`. Единственная точка доступа — `src/lib/i18n.ts` (`t()` + `applyI18n()`), которая переживает отсутствие глобального `chrome` и возвращает ключ. `KeeneticError` переходит с готовой русской прозы на ключи каталога, поэтому тесты начинают ассертить машинную идентичность ошибки, а не формулировку.

**Tech Stack:** TypeScript 5.7, Vite 6, `@crxjs/vite-plugin` 2.7, Manifest V3, встроенный тест-раннер Node (`node --test`) поверх esbuild-бандла.

## Global Constraints

- Спецификация: `docs/superpowers/specs/2026-09-07-i18n-design.md`.
- Локали: `en`, `ru`, `uk`, `pl`, `de`, `tr`. `default_locale` — **`en`**.
- Каталоги: `public/_locales/<locale>/messages.json`. Каталог `en` — источник правды по набору ключей.
- Подстановки — **только именованные плейсхолдеры** с блоком `placeholders` и `content: "$1"`. Голые `$1` в тексте сообщения официально не документированы и в этом проекте не используются.
- `chrome.i18n.getMessage` возвращает `''` для неизвестного ключа и `undefined` при некорректном вызове — фолбэк `|| key` покрывает оба случая.
- `name` в манифесте и `action.default_title` остаются литералом `'KeenSwitch'`: бренд не переводится.
- Лимит Chrome Web Store на `description` — **132 символа**, действует для каждой локали отдельно.
- В `chrome.i18n` нет множественного числа. Все счётные строки — в форме «Подпись: N» с двоеточием.
- Ключи каталога — `lowerCamelCase`.
- Комментарии в коде и `README.md` остаются на русском и в объём не входят.
- Каждая задача заканчивается зелёными `npm run typecheck`, `npm test`, `npm run build` и коммитом.

## Структура файлов

**Создаются:**

| Файл | Ответственность |
| --- | --- |
| `src/lib/i18n.ts` | Единственная точка доступа к переводам: `t()`, `applyI18n()`, `localizeDocument()` |
| `public/_locales/en/messages.json` | Каталог-источник правды по набору ключей |
| `public/_locales/ru/messages.json` | Русский каталог (перенос существующих текстов) |
| `public/_locales/{uk,pl,de,tr}/messages.json` | Остальные локали (задача 5) |
| `tests/i18n.test.ts` | Фолбэк `t()` без `chrome` и целостность каталогов |

**Изменяются:**

| Файл | Что меняется |
| --- | --- |
| `manifest.config.ts` | `default_locale: 'en'`, `description: '__MSG_appDesc__'` |
| `scripts/package.mjs` | Проверка длины `appDesc` по каждой локали вместо `manifest.description` |
| `src/lib/keenetic.ts` | `KeeneticError` на ключи; 23 строки → ключи; `assertNoRciError` принимает ключ действия |
| `src/lib/dnr.ts` | 1 строка → ключ |
| `src/lib/messages.ts` | 1 строка → ключ |
| `src/background.ts` | 4 строки → ключи |
| `src/popup/index.html` | `data-i18n`, снятие `lang="ru"` |
| `src/popup/popup.ts` | 12 строк → ключи, вызов `localizeDocument()` |
| `src/options/index.html` | `data-i18n`, снятие `lang="ru"` |
| `src/options/options.ts` | 16 строк → ключи, вызов `localizeDocument()` |
| `tests/auth.test.ts` | Ассерт по `error.key` вместо русского текста |
| `tests/keenetic.test.ts` | `assertNoRciError` вызывается с ключом действия |

## Порядок задач

Задачи 1–4 доводят расширение до полностью рабочего двуязычного состояния (en + ru): английский каталог пишется как источник правды, русский — переносом существующих текстов, ничего изобретать не нужно. Задача 5 добавляет оставшиеся четыре локали переводом готового английского каталога.

Тест целостности каталогов проверяет те локали, которые физически лежат в `public/_locales`, поэтому он зелёный на каждом коммите — и на двух локалях, и на шести.

---

### Task 1: Каркас i18n и локализованное описание в Store

**Files:**
- Create: `src/lib/i18n.ts`
- Create: `public/_locales/en/messages.json`
- Create: `public/_locales/ru/messages.json`
- Create: `tests/i18n.test.ts`
- Modify: `manifest.config.ts:5-10`
- Modify: `scripts/package.mjs:16-33`
- Modify: `scripts/test.mjs:13-22`

**Interfaces:**
- Consumes: ничего.
- Produces:
  - `t(key: string, subs?: string | string[]): string` — перевод по ключу; возвращает сам `key`, если `chrome.i18n` недоступен или ключ не найден.
  - `applyI18n(root?: ParentNode): void` — подставляет тексты в DOM по атрибутам `data-i18n`, `data-i18n-title`, `data-i18n-aria-label`, `data-i18n-placeholder`.
  - `localizeDocument(): void` — вызывает `applyI18n(document)` и выставляет `document.documentElement.lang`.
  - `public/_locales/en/messages.json` — каталог-источник правды; задачи 2–4 дописывают в него ключи.

- [ ] **Step 1: Расширить тест-раннер на все файлы `tests/*.test.ts`**

Сейчас `scripts/test.mjs` перечисляет тестовые файлы вручную, и новый `tests/i18n.test.ts` молча не запустится. Заменить в `scripts/test.mjs` жёсткий список на чтение каталога.

Было (строки 13–22):

```js
const outdir = '.test-build';

await rm(outdir, { recursive: true, force: true });
await build({
  entryPoints: ['tests/keenetic.test.ts', 'tests/auth.test.ts'],
  outdir,
```

Стало:

```js
const outdir = '.test-build';

// Список тестов читается с диска: иначе новый файл в tests/ молча не запустится.
const entryPoints = (await readdir('tests'))
  .filter((name) => name.endsWith('.test.ts'))
  .map((name) => `tests/${name}`);

await rm(outdir, { recursive: true, force: true });
await build({
  entryPoints,
  outdir,
```

`readdir` уже импортирован в первой строке файла — новый импорт не нужен.

- [ ] **Step 2: Написать падающий тест на фолбэк `t()` и целостность каталогов**

Создать `tests/i18n.test.ts`:

```ts
import assert from 'node:assert/strict';
import { readdir, readFile } from 'node:fs/promises';
import test from 'node:test';

import { t } from '../src/lib/i18n';

const LOCALES_DIR = 'public/_locales';

interface Entry {
  message: string;
  placeholders?: Record<string, { content: string }>;
}

/** readdir отдаёт и .DS_Store: без фильтра он попадёт в список локалей и уронит readCatalog. */
async function localeDirs(): Promise<string[]> {
  return (await readdir(LOCALES_DIR)).filter((name) => !name.startsWith('.'));
}

async function readCatalog(locale: string): Promise<Record<string, Entry>> {
  return JSON.parse(await readFile(`${LOCALES_DIR}/${locale}/messages.json`, 'utf8')) as Record<
    string,
    Entry
  >;
}

test('t() без глобального chrome возвращает ключ, а не падает', () => {
  // Тесты идут в голом Node: если t() полезет в chrome.i18n без проверки,
  // упадёт весь прогон на импорте keenetic.ts, а не один этот assert.
  assert.equal((globalThis as { chrome?: unknown }).chrome, undefined);
  assert.equal(t('errAuthRejected'), 'errAuthRejected');
  assert.equal(t('errRciAction', 'Смена политики'), 'errRciAction');
});

test('во всех каталогах одинаковый набор ключей', async () => {
  const locales = await localeDirs();
  const referenceKeys = Object.keys(await readCatalog('en')).sort();

  assert.ok(locales.includes('en'), 'каталог en обязателен: он источник правды');

  for (const locale of locales) {
    const keys = Object.keys(await readCatalog(locale)).sort();
    assert.deepEqual(keys, referenceKeys, `набор ключей в ${locale} разошёлся с en`);
  }
});

test('во всех каталогах одинаковые плейсхолдеры', async () => {
  const locales = await localeDirs();
  const reference = await readCatalog('en');

  for (const locale of locales) {
    const catalog = await readCatalog(locale);
    for (const [key, entry] of Object.entries(reference)) {
      assert.deepEqual(
        Object.keys(catalog[key].placeholders ?? {}).sort(),
        Object.keys(entry.placeholders ?? {}).sort(),
        `плейсхолдеры ключа ${key} в ${locale} разошлись с en`,
      );
    }
  }
});

test('appDesc укладывается в лимит Chrome Web Store во всех локалях', async () => {
  for (const locale of await localeDirs()) {
    const { appDesc } = await readCatalog(locale);
    assert.ok(
      appDesc.message.length <= 132,
      `appDesc в локали ${locale}: ${appDesc.message.length} символов при лимите 132`,
    );
  }
});
```

- [ ] **Step 3: Запустить тест и убедиться, что он падает**

Run: `npm test`
Expected: FAIL — esbuild не может разрешить импорт `../src/lib/i18n` (`Could not resolve "../src/lib/i18n"`).

- [ ] **Step 4: Создать `src/lib/i18n.ts`**

```ts
/**
 * Единственная точка доступа к каталогам переводов
 * (`public/_locales/<locale>/messages.json`).
 *
 * Расширение локализуется нативным chrome.i18n: язык интерфейса равен языку
 * интерфейса Chrome и внутри расширения не переключается — это осознанный
 * компромисс, см. docs/superpowers/specs/2026-09-07-i18n-design.md.
 *
 * Тесты выполняются в голом Node, где глобального `chrome` нет вовсе. Поэтому
 * доступ к API идёт через `api()` с проверкой: без неё импорт этого модуля в
 * keenetic.ts уронил бы весь тестовый прогон ещё до первого assert.
 */

interface I18nApi {
  getMessage(key: string, substitutions?: string | string[]): string;
  getUILanguage(): string;
}

/** Локальный интерфейс вместо typeof chrome.i18n: модуль не должен зависеть от наличия @types/chrome в рантайме тестов. */
function api(): I18nApi | undefined {
  return (globalThis as { chrome?: { i18n?: I18nApi } }).chrome?.i18n;
}

/**
 * Перевод по ключу. `getMessage` отдаёт '' для неизвестного ключа и undefined
 * при некорректном вызове — оба случая схлопываются в сам ключ, чтобы в UI
 * было видно, чего не хватает, а не пустое место.
 */
export function t(key: string, subs?: string | string[]): string {
  return api()?.getMessage(key, subs) || key;
}

/** Атрибут в разметке -> атрибут, куда пишется перевод. */
const ATTRIBUTE_TARGETS: readonly (readonly [string, string])[] = [
  ['data-i18n-title', 'title'],
  ['data-i18n-aria-label', 'aria-label'],
  ['data-i18n-placeholder', 'placeholder'],
];

/** Подставляет тексты в статическую разметку. */
export function applyI18n(root: ParentNode = document): void {
  for (const element of root.querySelectorAll<HTMLElement>('[data-i18n]')) {
    element.textContent = t(element.getAttribute('data-i18n') ?? '');
  }

  for (const [source, target] of ATTRIBUTE_TARGETS) {
    for (const element of root.querySelectorAll<HTMLElement>(`[${source}]`)) {
      element.setAttribute(target, t(element.getAttribute(source) ?? ''));
    }
  }
}

/** Локализует страницу целиком: тексты плюс `lang` для переносов и скринридеров. */
export function localizeDocument(): void {
  document.documentElement.lang = api()?.getUILanguage() ?? 'en';
  applyI18n(document);
}
```

- [ ] **Step 5: Создать каталоги `en` и `ru` с ключом `appDesc`**

`public/_locales/en/messages.json`:

```json
{
  "appDesc": {
    "message": "Switch this device between Keenetic router access policies in one click."
  }
}
```

`public/_locales/ru/messages.json`:

```json
{
  "appDesc": {
    "message": "Переключение текущего устройства между политиками доступа (Policy) на роутере Keenetic в один клик."
  }
}
```

- [ ] **Step 6: Запустить тесты — должны пройти**

Run: `npm test`
Expected: PASS, четыре новых теста из `i18n.test.ts` зелёные.

- [ ] **Step 7: Перевести манифест на `__MSG_appDesc__`**

В `manifest.config.ts` заменить блок (строки 5–10):

```ts
export default defineManifest({
  manifest_version: 3,
  name: 'KeenSwitch',
  version: pkg.version,
  description:
    'Переключение текущего устройства между политиками доступа (Policy) на роутере Keenetic в один клик.',
```

на:

```ts
export default defineManifest({
  manifest_version: 3,
  // Бренд не переводится: название должно совпадать во всех витринах Store.
  name: 'KeenSwitch',
  version: pkg.version,
  default_locale: 'en',
  description: '__MSG_appDesc__',
```

- [ ] **Step 8: Собрать и проверить, что каталоги попали в `dist`**

Run: `npm run build && ls dist/_locales/*/messages.json && grep -o '"description":"[^"]*"' dist/manifest.json`
Expected: две строки путей (`dist/_locales/en/messages.json`, `dist/_locales/ru/messages.json`) и `"description":"__MSG_appDesc__"`.

Если `dist/_locales` пуст — значит Vite не подхватил `public/`; проверить, что файлы лежат именно в `public/_locales/`, а не в `src/`.

- [ ] **Step 9: Перевести проверку длины описания в `scripts/package.mjs` на локали**

После замены на `__MSG_appDesc__` проверка `manifest.description.length > 132` меряет длину плейсхолдера (15 символов) и всегда проходит. Лимит Store действует на каждую локаль отдельно.

Заменить в `scripts/package.mjs` блок (строки 16–33):

```js
import { readFile, rm } from 'node:fs/promises';

const pkg = JSON.parse(await readFile('package.json', 'utf8'));
const manifest = JSON.parse(await readFile('dist/manifest.json', 'utf8'));

if (manifest.version !== pkg.version) {
  throw new Error(`Версии разошлись: package.json ${pkg.version}, dist/manifest.json ${manifest.version}. Пересоберите: npm run build`);
}
if (manifest.description.length > 132) {
  throw new Error(`Описание длиннее 132 символов (${manifest.description.length}) — Chrome Web Store его не примет`);
}
```

на:

```js
import { readdir, readFile, rm } from 'node:fs/promises';

const DESCRIPTION_LIMIT = 132;

const pkg = JSON.parse(await readFile('package.json', 'utf8'));
const manifest = JSON.parse(await readFile('dist/manifest.json', 'utf8'));

if (manifest.version !== pkg.version) {
  throw new Error(`Версии разошлись: package.json ${pkg.version}, dist/manifest.json ${manifest.version}. Пересоберите: npm run build`);
}

// description в манифесте — это __MSG_appDesc__, мерить его длину бессмысленно.
// Store применяет лимит к описанию каждой локали по отдельности.
const locales = (await readdir('dist/_locales')).filter((name) => !name.startsWith('.'));
if (locales.length === 0) {
  throw new Error('В dist/_locales нет ни одной локали. Пересоберите: npm run build');
}
for (const locale of locales) {
  const catalog = JSON.parse(await readFile(`dist/_locales/${locale}/messages.json`, 'utf8'));
  const length = catalog.appDesc.message.length;
  if (length > DESCRIPTION_LIMIT) {
    throw new Error(`Описание локали ${locale} длиннее ${DESCRIPTION_LIMIT} символов (${length}) — Chrome Web Store его не примет`);
  }
}
```

Ниже в том же файле, в финальном `console.log`, заменить строку

```js
console.log(`Версия: ${manifest.version} · описание: ${manifest.description.length}/132 символов`);
```

на

```js
console.log(`Версия: ${manifest.version} · локалей: ${locales.length} (${locales.join(', ')})`);
```

- [ ] **Step 10: Проверить упаковку целиком**

Run: `npm run package`
Expected: архив собирается, в листинге видны `_locales/en/messages.json` и `_locales/ru/messages.json`, в конце — `локалей: 2 (en, ru)`.

- [ ] **Step 11: Полная проверка и коммит**

Run: `npm run typecheck && npm test && npm run build`
Expected: всё зелёное.

```bash
git add src/lib/i18n.ts public/_locales tests/i18n.test.ts manifest.config.ts scripts/package.mjs scripts/test.mjs
git commit -m "feat(i18n): каркас переводов и локализованное описание в Store

Добавляет src/lib/i18n.ts с фолбэком на ключ при отсутствии chrome.i18n —
без него импорт t() в keenetic.ts уронил бы тестовый прогон в Node.

Проверка лимита 132 символа переезжает с manifest.description (после
замены на __MSG_appDesc__ там плейсхолдер) на каждую локаль отдельно:
Store применяет лимит к локализованному описанию, а не к манифесту."
```

---

### Task 2: Слой ошибок переходит с прозы на ключи

**Files:**
- Modify: `src/lib/keenetic.ts:25-32` (класс), далее все места выброса
- Modify: `src/lib/dnr.ts:62-67`
- Modify: `src/lib/messages.ts:37`
- Modify: `src/background.ts:33-42`
- Modify: `public/_locales/en/messages.json`
- Modify: `public/_locales/ru/messages.json`
- Modify: `tests/auth.test.ts:82-89`
- Modify: `tests/keenetic.test.ts:38-45`

**Interfaces:**
- Consumes: `t()` из `src/lib/i18n.ts` (задача 1).
- Produces:
  - `class KeeneticError extends Error` с полями `kind: ErrorKind`, `key: string`, `detail?: string` и конструктором `(kind: ErrorKind, key: string, options?: KeeneticErrorOptions)`.
  - `interface KeeneticErrorOptions { subs?: string | string[]; detailKey?: string; detailSubs?: string | string[]; detailText?: string }`.
  - `assertNoRciError(value: unknown, actionKey: string): void` — второй аргумент теперь **ключ каталога**, а не подпись действия.

- [ ] **Step 1: Переписать ассерты в тестах на ключи (падающий тест)**

В `tests/auth.test.ts` заменить блок (строки 82–89):

```ts
  await assert.rejects(
    authenticate(settings),
    (error: unknown) =>
      error instanceof KeeneticError &&
      error.kind === 'auth' &&
      error.message === 'Роутер отклонил логин и пароль' &&
      String(error.detail).includes('веб-панель'),
  );
```

на:

```ts
  await assert.rejects(
    authenticate(settings),
    // Ассерт по ключу, а не по формулировке: текст живёт в _locales и меняется
    // без участия теста, идентичность ошибки — нет.
    (error: unknown) =>
      error instanceof KeeneticError &&
      error.kind === 'auth' &&
      error.key === 'errAuthRejected' &&
      error.detail === 'errAuthRejectedDetail',
  );
```

В `tests/keenetic.test.ts` заменить блок (строки 38–45):

```ts
test('assertNoRciError различает ошибки и предупреждения', () => {
  assert.doesNotThrow(() => assertNoRciError({ status: [{ status: 'message', message: 'ok' }] }, 'x'));
  assert.throws(
    () => assertNoRciError({ status: [{ status: 'error', message: 'policy not found' }] }, 'Смена политики'),
    (error: unknown) =>
      error instanceof KeeneticError && error.kind === 'rci' && error.detail === 'policy not found',
  );
});
```

на:

```ts
test('assertNoRciError различает ошибки и предупреждения', () => {
  assert.doesNotThrow(() =>
    assertNoRciError({ status: [{ status: 'message', message: 'ok' }] }, 'actionSetPolicy'),
  );
  assert.throws(
    () =>
      assertNoRciError(
        { status: [{ status: 'error', message: 'policy not found' }] },
        'actionSetPolicy',
      ),
    // detail — сырой текст роутера, он не переводится и проходит насквозь.
    (error: unknown) =>
      error instanceof KeeneticError &&
      error.kind === 'rci' &&
      error.key === 'errRciAction' &&
      error.detail === 'policy not found',
  );
});
```

- [ ] **Step 2: Запустить тесты и убедиться, что они падают**

Run: `npm test`
Expected: FAIL — TypeScript-бандл соберётся, но оба теста упадут: у `KeeneticError` нет поля `key`, а `error.detail` в auth-тесте равен русской прозе, а не `'errAuthRejectedDetail'`.

- [ ] **Step 3: Переписать класс `KeeneticError`**

В `src/lib/keenetic.ts` добавить импорт после строки `import { md5Hex } from './md5';`:

```ts
import { t } from './i18n';
```

Заменить блок (строки 25–32):

```ts
export class KeeneticError extends Error {
  constructor(
    readonly kind: ErrorKind,
    message: string,
    readonly detail?: string,
  ) {
    super(message);
    this.name = 'KeeneticError';
  }
}
```

на:

```ts
export interface KeeneticErrorOptions {
  /** Подстановки в основное сообщение. */
  subs?: string | string[];
  /** Ключ каталога для пояснения. */
  detailKey?: string;
  /** Подстановки в пояснение. */
  detailSubs?: string | string[];
  /** Готовое пояснение от роутера или браузера — диагностика, она не переводится. */
  detailText?: string;
}

/**
 * Ошибка несёт ключ каталога, а не готовый текст: `key` — машинная
 * идентичность ошибки, по ней ассертят тесты и ветвится UI, а формулировка
 * живёт в _locales и меняется независимо.
 */
export class KeeneticError extends Error {
  readonly detail?: string;

  constructor(
    readonly kind: ErrorKind,
    readonly key: string,
    options: KeeneticErrorOptions = {},
  ) {
    super(t(key, options.subs));
    this.name = 'KeeneticError';
    this.detail =
      options.detailText ??
      (options.detailKey ? t(options.detailKey, options.detailSubs) : undefined);
  }
}
```

- [ ] **Step 4: Перевести места выброса в `keenetic.ts` на ключи**

Заменить построчно (порядок сверху вниз по файлу):

`routerFetch`:

```ts
    throw new KeeneticError('network', 'errRouterUnreachable', {
      detailText: cause instanceof Error ? cause.message : String(cause),
    });
```

`performHandshake`:

```ts
    throw new KeeneticError('auth', 'errAuthUnexpectedStatus', { subs: String(probe.status) });
```

```ts
    throw new KeeneticError('auth', 'errNoChallenge', { detailKey: 'errNoChallengeDetail' });
```

```ts
    throw new KeeneticError('config', 'errPasswordNotSet');
```

```ts
    throw new KeeneticError('auth', 'errAuthFailedStatus', { subs: String(response.status) });
```

```ts
    throw new KeeneticError('auth', 'errSessionNotStored', {
      detailKey: 'errSessionNotStoredDetail',
    });
```

`performAuthentication`:

```ts
  throw new KeeneticError('auth', 'errAuthRejected', { detailKey: 'errAuthRejectedDetail' });
```

`assertNoRciError` — сменить сигнатуру и выброс:

```ts
/** Ищет в ответе RCI записи `status` с уровнем `error` и превращает их в исключение. */
export function assertNoRciError(value: unknown, actionKey: string): void {
```

```ts
  throw new KeeneticError('rci', 'errRciAction', { subs: t(actionKey), detailText: message });
```

`rci`:

```ts
    throw new KeeneticError('auth', 'errSessionExpired');
```

```ts
    throw new KeeneticError('rci', 'errForbidden', { detailKey: 'errForbiddenDetail' });
```

```ts
    throw new KeeneticError('rci', 'errRciHttp', { subs: String(response.status) });
```

```ts
    throw new KeeneticError('rci', 'errBadResponse');
```

`fetchDeviceState`:

```ts
  if (!mac) {
    throw new KeeneticError(
      'config',
      settings.autoDetectDevice ? 'errWhoamiFailed' : 'errNoDeviceSelected',
    );
  }
```

`setDevicePolicy` — вызовы `assertNoRciError` и финальная проверка:

```ts
  assertNoRciError(writeResult, 'actionSetPolicy');
  assertNoRciError(saveResult, 'actionSaveConfig');
```

```ts
  if (state.currentPolicyId !== policyId) {
    throw new KeeneticError('rci', 'errPolicyNotApplied', {
      detailKey: 'errPolicyNotAppliedDetail',
      detailSubs: [
        policyId ?? t('policyNoneInline'),
        state.currentPolicyId ?? t('policyNoneInline'),
      ],
    });
  }
```

- [ ] **Step 5: Перевести `dnr.ts`, `messages.ts` и `background.ts`**

В `src/lib/dnr.ts` добавить импорт сразу после комментария-шапки, перед `const RULE_ID = 1;`:

```ts
import { t } from './i18n';
```

и заменить тело `catch` в `updateRules`:

```ts
  } catch (cause) {
    throw new Error(
      t('errOriginRule', cause instanceof Error ? cause.message : String(cause)),
    );
  }
```

В `src/lib/messages.ts` добавить импорт после существующих:

```ts
import { t } from './i18n';
```

и заменить строку 37:

```ts
    throw new Error(t('errNoBackgroundResponse'));
```

В `src/background.ts` заменить тело проверок в `requireReadySettings`:

```ts
  if (!settings.baseUrl) {
    throw new KeeneticError('config', 'errBaseUrlNotSet');
  }
  if (!settings.password) {
    throw new KeeneticError('config', 'errPasswordNotSet');
  }
  if (!(await hasRouterPermission(settings.baseUrl))) {
    throw new KeeneticError('permission', 'errNoPermission', {
      detailKey: 'errNoPermissionDetail',
      detailSubs: settings.baseUrl,
    });
  }
```

Сообщение `Неизвестный запрос: …` в `default`-ветке `switch` не трогать: это недостижимая ветка исчерпывающей проверки типов, пользователь её не видит.

- [ ] **Step 6: Дописать 28 ключей в каталог `en`**

Добавить в `public/_locales/en/messages.json` (рядом с `appDesc`):

```json
  "errRouterUnreachable": { "message": "The router is unreachable" },
  "errAuthUnexpectedStatus": {
    "message": "Unexpected response from /auth: HTTP $status$",
    "placeholders": { "status": { "content": "$1" } }
  },
  "errNoChallenge": { "message": "The router did not send an authentication challenge" },
  "errNoChallengeDetail": {
    "message": "This address appears to serve something other than the KeeneticOS web interface."
  },
  "errPasswordNotSet": { "message": "The router password is not set" },
  "errAuthFailedStatus": {
    "message": "Authentication failed: HTTP $status$",
    "placeholders": { "status": { "content": "$1" } }
  },
  "errSessionNotStored": { "message": "The router accepted the password, but the session was not stored" },
  "errSessionNotStoredDetail": {
    "message": "The browser is not sending the router's session cookie. Check that permission for the router address is granted in the extension settings."
  },
  "errAuthRejected": { "message": "The router rejected the username and password" },
  "errAuthRejectedDetail": {
    "message": "If the credentials are definitely correct, close the router web interface tab: it refreshes the session and stops the extension from signing in."
  },
  "errRciAction": {
    "message": "$action$: the router returned an error",
    "placeholders": { "action": { "content": "$1" } }
  },
  "actionSetPolicy": { "message": "Switching the policy" },
  "actionSaveConfig": { "message": "Saving the configuration" },
  "errSessionExpired": { "message": "The router session has expired" },
  "errForbidden": { "message": "The router rejected the request (403)" },
  "errForbiddenDetail": {
    "message": "KeeneticOS only accepts requests to /rci/ that carry its own Origin. The extension rewrites the header with a declarativeNetRequest rule — check that permission for the router address is granted in chrome://extensions and reload the extension."
  },
  "errRciHttp": {
    "message": "Request to the router failed: HTTP $status$",
    "placeholders": { "status": { "content": "$1" } }
  },
  "errBadResponse": { "message": "Unexpected response format from the router" },
  "errWhoamiFailed": { "message": "The router could not identify this device" },
  "errNoDeviceSelected": { "message": "No device is selected in the settings" },
  "errPolicyNotApplied": { "message": "The router did not apply the new policy" },
  "errPolicyNotAppliedDetail": {
    "message": "Expected \"$expected$\", but after the write the router reports \"$actual$\".",
    "placeholders": { "expected": { "content": "$1" }, "actual": { "content": "$2" } }
  },
  "policyNoneInline": { "message": "no policy" },
  "errOriginRule": {
    "message": "Could not install the Origin rewrite rule: $reason$",
    "placeholders": { "reason": { "content": "$1" } }
  },
  "errNoBackgroundResponse": { "message": "The extension background script did not respond" },
  "errBaseUrlNotSet": { "message": "The router address is not set" },
  "errNoPermission": { "message": "No access to the router address" },
  "errNoPermissionDetail": {
    "message": "Open the extension settings and grant permission for $url$.",
    "placeholders": { "url": { "content": "$1" } }
  }
```

- [ ] **Step 7: Дописать те же 28 ключей в каталог `ru`**

Тексты переносятся из кода один в один, ничего переформулировать не нужно. Добавить в `public/_locales/ru/messages.json`:

```json
  "errRouterUnreachable": { "message": "Роутер недоступен" },
  "errAuthUnexpectedStatus": {
    "message": "Неожиданный ответ на /auth: HTTP $status$",
    "placeholders": { "status": { "content": "$1" } }
  },
  "errNoChallenge": { "message": "Роутер не прислал challenge для авторизации" },
  "errNoChallengeDetail": {
    "message": "Похоже, по этому адресу отвечает не веб-интерфейс KeeneticOS."
  },
  "errPasswordNotSet": { "message": "Не задан пароль роутера" },
  "errAuthFailedStatus": {
    "message": "Авторизация не прошла: HTTP $status$",
    "placeholders": { "status": { "content": "$1" } }
  },
  "errSessionNotStored": { "message": "Роутер принял пароль, но сессия не сохранилась" },
  "errSessionNotStoredDetail": {
    "message": "Браузер не отдаёт сессионную cookie роутера. Проверьте, что для адреса роутера выдано разрешение в настройках расширения."
  },
  "errAuthRejected": { "message": "Роутер отклонил логин и пароль" },
  "errAuthRejectedDetail": {
    "message": "Если логин и пароль точно верные, закройте вкладку с веб-панелью роутера: она обновляет сессию и мешает расширению авторизоваться."
  },
  "errRciAction": {
    "message": "$action$: роутер вернул ошибку",
    "placeholders": { "action": { "content": "$1" } }
  },
  "actionSetPolicy": { "message": "Смена политики" },
  "actionSaveConfig": { "message": "Сохранение конфигурации" },
  "errSessionExpired": { "message": "Сессия роутера истекла" },
  "errForbidden": { "message": "Роутер отклонил запрос (403)" },
  "errForbiddenDetail": {
    "message": "KeeneticOS пропускает запросы к /rci/ только со своим собственным Origin. Расширение подменяет заголовок правилом declarativeNetRequest — проверьте, что в chrome://extensions выдано разрешение на адрес роутера, и перезагрузите расширение."
  },
  "errRciHttp": {
    "message": "Запрос к роутеру не прошёл: HTTP $status$",
    "placeholders": { "status": { "content": "$1" } }
  },
  "errBadResponse": { "message": "Неожиданный формат ответа роутера" },
  "errWhoamiFailed": { "message": "Роутер не смог определить это устройство" },
  "errNoDeviceSelected": { "message": "Устройство не выбрано в настройках" },
  "errPolicyNotApplied": { "message": "Роутер не применил новую политику" },
  "errPolicyNotAppliedDetail": {
    "message": "Ожидалось «$expected$», после записи роутер сообщает «$actual$».",
    "placeholders": { "expected": { "content": "$1" }, "actual": { "content": "$2" } }
  },
  "policyNoneInline": { "message": "без политики" },
  "errOriginRule": {
    "message": "Не удалось установить правило подмены Origin: $reason$",
    "placeholders": { "reason": { "content": "$1" } }
  },
  "errNoBackgroundResponse": { "message": "Фоновый скрипт расширения не ответил" },
  "errBaseUrlNotSet": { "message": "Не указан адрес роутера" },
  "errNoPermission": { "message": "Нет доступа к адресу роутера" },
  "errNoPermissionDetail": {
    "message": "Откройте настройки расширения и выдайте разрешение для $url$.",
    "placeholders": { "url": { "content": "$1" } }
  }
```

- [ ] **Step 8: Запустить тесты — должны пройти**

Run: `npm test`
Expected: PASS. Тест целостности каталогов подтверждает, что en и ru совпали по ключам и плейсхолдерам; auth-тест проходит по `error.key`.

- [ ] **Step 9: Полная проверка и коммит**

Run: `npm run typecheck && npm test && npm run build`
Expected: всё зелёное.

```bash
git add src/lib/keenetic.ts src/lib/dnr.ts src/lib/messages.ts src/background.ts public/_locales tests/auth.test.ts tests/keenetic.test.ts
git commit -m "refactor(i18n): ошибки несут ключ каталога вместо готового текста

KeeneticError получает поле key — машинную идентичность ошибки. Тесты
переходят на ассерт по ключу и перестают ломаться от правки формулировки.

Сырой текст от роутера (detailText) проходит насквозь без перевода: это
диагностика устройства, а не наш интерфейс."
```

---

### Task 3: Попап

**Files:**
- Modify: `src/popup/index.html:2`, `:41`, `:69`, `:73`
- Modify: `src/popup/popup.ts`
- Modify: `public/_locales/en/messages.json`
- Modify: `public/_locales/ru/messages.json`

**Interfaces:**
- Consumes: `t()`, `localizeDocument()` из `src/lib/i18n.ts` (задача 1).
- Produces: ничего для последующих задач.

- [ ] **Step 1: Дописать 15 ключей в каталог `en`**

```json
  "optionsButton": { "message": "Settings" },
  "popupConnecting": { "message": "Connecting to the router…" },
  "popupPoliciesTitle": { "message": "Access policy" },
  "openOptions": { "message": "Open settings" },
  "chipBlocked": { "message": "access denied" },
  "chipOffline": { "message": "offline" },
  "chipManual": { "message": "manual" },
  "policyNone": { "message": "No policy" },
  "policyDefaultSub": {
    "message": "default policy: $policy$",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "policyNoneSub": { "message": "shared access, same as other devices" },
  "statusSwitching": {
    "message": "Switching to \"$policy$\"…",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "statusSwitched": {
    "message": "Done: \"$policy$\"",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "noPolicies": {
    "message": "No access policies are configured on the router — create one in the Keenetic web interface."
  },
  "deviceBlocked": { "message": "This device is denied internet access in the router settings." },
  "noRouterConnection": { "message": "No connection to the router" }
```

- [ ] **Step 2: Дописать те же 15 ключей в каталог `ru`**

```json
  "optionsButton": { "message": "Настройки" },
  "popupConnecting": { "message": "Подключаюсь к роутеру…" },
  "popupPoliciesTitle": { "message": "Политика доступа" },
  "openOptions": { "message": "Открыть настройки" },
  "chipBlocked": { "message": "доступ запрещён" },
  "chipOffline": { "message": "нет в сети" },
  "chipManual": { "message": "вручную" },
  "policyNone": { "message": "Без политики" },
  "policyDefaultSub": {
    "message": "политика по умолчанию: $policy$",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "policyNoneSub": { "message": "общий доступ, как у остальных устройств" },
  "statusSwitching": {
    "message": "Переключаю на «$policy$»…",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "statusSwitched": {
    "message": "Готово: «$policy$»",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "noPolicies": {
    "message": "На роутере не настроено ни одной политики доступа — создайте её в панели Keenetic."
  },
  "deviceBlocked": { "message": "Этому устройству запрещён доступ в интернет в настройках роутера." },
  "noRouterConnection": { "message": "Нет связи с роутером" }
```

- [ ] **Step 3: Проверить целостность каталогов**

Run: `npm test`
Expected: PASS — наборы ключей en и ru совпадают.

- [ ] **Step 4: Разметить `src/popup/index.html`**

Строка 2: `<html lang="ru">` → `<html lang="en">`.

Статический текст в разметке пишется по-английски: `default_locale` — `en`, и если JS почему-то не отработал, пользователь увидит осмысленный английский, а не кириллицу.

Кнопка настроек (строка 41):

```html
      <button
        id="open-options"
        class="icon-button"
        type="button"
        data-i18n-aria-label="optionsButton"
        data-i18n-title="optionsButton"
        aria-label="Settings"
        title="Settings"
      >
```

Строка 69:

```html
      <span class="visually-hidden" data-i18n="popupConnecting">Connecting to the router…</span>
```

Строка 73:

```html
      <h2 id="policies-title" data-i18n="popupPoliciesTitle">Access policy</h2>
```

- [ ] **Step 5: Перевести `src/popup/popup.ts` на ключи**

Добавить импорт после `import { icon, type IconName } from '../lib/icons';`:

```ts
import { localizeDocument, t } from '../lib/i18n';
```

Заменить строки в `renderDevice` (блок с плашкой):

```ts
  if (state.blocked) {
    chip.dataset.tone = 'danger';
    chip.textContent = t('chipBlocked');
  } else if (!state.known) {
    chip.dataset.tone = 'warn';
    chip.textContent = t('chipOffline');
  } else if (state.detectedBy === 'manual') {
    chip.textContent = t('chipManual');
  }
```

В `renderPolicies` — блок `options`:

```ts
  const options: { id: string | null; label: string; sub?: string }[] = [
    {
      id: null,
      label: t('policyNone'),
      sub: defaultPolicyLabel ? t('policyDefaultSub', defaultPolicyLabel) : t('policyNoneSub'),
    },
    ...state.policies.map((policy) => ({ id: policy.id as string | null, label: policy.label })),
  ];
```

В `reportError`:

```ts
  if (kind === 'config' || kind === 'permission') {
    addStatusAction(t('openOptions'), () => chrome.runtime.openOptionsPage());
  }
```

В `switchPolicy` — кавычки-ёлочки теперь живут внутри сообщения:

```ts
  lockPolicies(button);
  setStatus(t('statusSwitching', label), 'muted');

  try {
    const updated = await send({ type: 'setPolicy', mac: state.mac, policyId });
    renderDevice(updated);
    renderPolicies(updated);
    setStatus(t('statusSwitched', label), 'ok');
```

В `renderEmptyPolicies`:

```ts
  const text = document.createElement('span');
  text.textContent = t('noPolicies');
```

В `init` — сообщение о блокировке и заголовок ошибки связи:

```ts
    if (state.blocked) {
      setStatus(t('deviceBlocked'), 'error');
    }
```

```ts
    name.textContent = t('noRouterConnection');
```

Последней строкой файла заменить:

```ts
void init();
```

на:

```ts
// Статическая разметка локализуется до первого запроса к роутеру: скелетон
// показывается раньше, чем придёт ответ.
localizeDocument();
void init();
```

- [ ] **Step 6: Убедиться, что русских строк в попапе не осталось**

Run: `grep -n "[А-Яа-яЁё]" src/popup/popup.ts src/popup/index.html`
Expected: только строки комментариев (`/** … */`, `//`). Ни одного литерала в кавычках и ни одного текста в разметке.

- [ ] **Step 7: Полная проверка и коммит**

Run: `npm run typecheck && npm test && npm run build`
Expected: всё зелёное.

```bash
git add src/popup public/_locales
git commit -m "feat(i18n): локализация попапа

Статический текст в разметке — по-английски: default_locale = en, и если
JS не отработает, пользователь увидит осмысленный английский, а не кириллицу."
```

---

### Task 4: Страница настроек

**Files:**
- Modify: `src/options/index.html`
- Modify: `src/options/options.ts`
- Modify: `public/_locales/en/messages.json`
- Modify: `public/_locales/ru/messages.json`

**Interfaces:**
- Consumes: `t()`, `localizeDocument()` из `src/lib/i18n.ts` (задача 1).
- Produces: ничего для последующих задач.

**Отдельно про два места с `<code>` внутри текста.**

В разметке есть два абзаца, где технический идентификатор стоит внутри предложения:

1. `<code>chrome.storage.local</code> доступен любому…` — идентификатор идёт **первым**, поэтому остаётся отдельным элементом, а следующая за ним проза переезжает в собственный `<span data-i18n="passwordWarnBody">`. Во всех шести языках конструкция «идентификатор + сказуемое» строится нормально, порядок слов не ломается.
2. `…с которого пришёл запрос (RCI <code>whoami</code>). Работает…` — здесь идентификатор стоит **в середине** предложения, и разрезать фразу на части значило бы жёстко зафиксировать порядок слов для всех языков. Поэтому элемент `<code>` тут убирается, а слово `whoami` уходит в текст сообщения обычным словом. Осознанная потеря моноширинного начертания на одном слове в обмен на переводимую фразу.

- [ ] **Step 1: Дописать 22 ключа разметки в каталог `en`**

```json
  "optionsTitle": { "message": "KeenSwitch settings" },
  "optionsIntro": {
    "message": "Two steps: connect to the router, then say which device to switch."
  },
  "sectionRouterTitle": { "message": "1. Router" },
  "sectionRouterSub": { "message": "Sign-in details for the Keenetic web interface." },
  "fieldBaseUrl": { "message": "Web interface address" },
  "fieldBaseUrlHint": { "message": "The same address you use to open the Keenetic panel." },
  "fieldLogin": { "message": "Username" },
  "fieldPassword": { "message": "Password" },
  "passwordShow": { "message": "Show password" },
  "passwordHide": { "message": "Hide password" },
  "passwordWarnTitle": { "message": "The password is stored in plain text" },
  "passwordWarnBody": {
    "message": "is readable by anyone with access to your Chrome profile. The password is sent only to the address you specified, and only as a hash (SHA-256 of the challenge)."
  },
  "testConnection": { "message": "Test connection" },
  "sectionDeviceTitle": { "message": "2. My device" },
  "sectionDeviceSub": { "message": "This is the device whose policy will be switched." },
  "modeAutoTitle": { "message": "Detect automatically" },
  "modeAutoHint": {
    "message": "The router reports the MAC address the request came from (RCI whoami). Works as long as the browser reaches the router directly."
  },
  "modeManualTitle": { "message": "Pick manually from the list" },
  "modeManualHint": {
    "message": "Needed when auto-detection returns the wrong MAC address (VPN, bridges, docking stations)."
  },
  "fieldDevice": { "message": "Device" },
  "deviceListPlaceholder": { "message": "— the list loads after you test the connection —" },
  "save": { "message": "Save" }
```

- [ ] **Step 2: Дописать 16 ключей из `options.ts` в каталог `en`**

```json
  "deviceHintNeedTest": { "message": "Press \"Test connection\" to load devices from the router." },
  "deviceListEmpty": { "message": "— the router returned no devices —" },
  "markThisDevice": { "message": "this device" },
  "markOffline": { "message": "offline" },
  "permNeedUrl": { "message": "Enter the router address." },
  "permGranted": {
    "message": "Access to $url$ is granted.",
    "placeholders": { "url": { "content": "$1" } }
  },
  "permNotGranted": {
    "message": "Access to $url$ is not granted yet — press \"Test connection\".",
    "placeholders": { "url": { "content": "$1" } }
  },
  "badUrl": { "message": "Cannot read the router address." },
  "needPassword": { "message": "Enter the router password." },
  "testing": { "message": "Testing…" },
  "permDenied": { "message": "Without access to the router address the extension cannot work." },
  "testOkWhoami": {
    "message": "Connected. Devices: $count$. This device: $device$.",
    "placeholders": { "count": { "content": "$1" }, "device": { "content": "$2" } }
  },
  "testOkNoWhoami": {
    "message": "Connected. Devices: $count$. Auto-detection did not work — pick the device manually.",
    "placeholders": { "count": { "content": "$1" } }
  },
  "needDevice": { "message": "Pick a device or turn on auto-detection." },
  "saving": { "message": "Saving…" },
  "saved": { "message": "Saved." }
```

- [ ] **Step 3: Дописать те же 38 ключей в каталог `ru`**

Тексты переносятся из существующей разметки и кода:

```json
  "optionsTitle": { "message": "Настройки KeenSwitch" },
  "optionsIntro": {
    "message": "Два шага: подключиться к роутеру и указать, какое устройство переключать."
  },
  "sectionRouterTitle": { "message": "1. Роутер" },
  "sectionRouterSub": { "message": "Данные для входа в веб-панель Keenetic." },
  "fieldBaseUrl": { "message": "Адрес веб-интерфейса" },
  "fieldBaseUrlHint": { "message": "Тот же адрес, по которому вы открываете панель Keenetic." },
  "fieldLogin": { "message": "Логин" },
  "fieldPassword": { "message": "Пароль" },
  "passwordShow": { "message": "Показать пароль" },
  "passwordHide": { "message": "Скрыть пароль" },
  "passwordWarnTitle": { "message": "Пароль хранится в открытом виде" },
  "passwordWarnBody": {
    "message": "доступен любому, у кого есть доступ к вашему профилю Chrome. На роутер пароль уходит только по указанному адресу и только в виде хеша (SHA-256 от challenge)."
  },
  "testConnection": { "message": "Проверить подключение" },
  "sectionDeviceTitle": { "message": "2. Моё устройство" },
  "sectionDeviceSub": { "message": "Политику будем переключать именно для него." },
  "modeAutoTitle": { "message": "Определять автоматически" },
  "modeAutoHint": {
    "message": "Роутер сам сообщает MAC устройства, с которого пришёл запрос (RCI whoami). Работает, пока браузер ходит к роутеру напрямую."
  },
  "modeManualTitle": { "message": "Выбрать вручную из списка" },
  "modeManualHint": {
    "message": "Нужно, если автоопределение даёт не тот MAC (VPN, мосты, докстанции)."
  },
  "fieldDevice": { "message": "Устройство" },
  "deviceListPlaceholder": { "message": "— список загружается после проверки подключения —" },
  "save": { "message": "Сохранить" },
  "deviceHintNeedTest": {
    "message": "Нажмите «Проверить подключение», чтобы загрузить устройства с роутера."
  },
  "deviceListEmpty": { "message": "— роутер не вернул ни одного устройства —" },
  "markThisDevice": { "message": "это устройство" },
  "markOffline": { "message": "не в сети" },
  "permNeedUrl": { "message": "Укажите адрес роутера." },
  "permGranted": {
    "message": "Доступ к $url$ разрешён.",
    "placeholders": { "url": { "content": "$1" } }
  },
  "permNotGranted": {
    "message": "Доступ к $url$ ещё не разрешён — нажмите «Проверить подключение».",
    "placeholders": { "url": { "content": "$1" } }
  },
  "badUrl": { "message": "Не понимаю адрес роутера." },
  "needPassword": { "message": "Введите пароль роутера." },
  "testing": { "message": "Проверяю…" },
  "permDenied": { "message": "Без доступа к адресу роутера расширение работать не сможет." },
  "testOkWhoami": {
    "message": "Связь есть. Устройств: $count$. Это устройство: $device$.",
    "placeholders": { "count": { "content": "$1" }, "device": { "content": "$2" } }
  },
  "testOkNoWhoami": {
    "message": "Связь есть. Устройств: $count$. Автоопределение не сработало — выберите устройство вручную.",
    "placeholders": { "count": { "content": "$1" } }
  },
  "needDevice": { "message": "Выберите устройство или включите автоопределение." },
  "saving": { "message": "Сохраняю…" },
  "saved": { "message": "Сохранено." }
```

- [ ] **Step 4: Проверить целостность каталогов**

Run: `npm test`
Expected: PASS — 82 ключа в обоих каталогах, плейсхолдеры совпадают.

- [ ] **Step 5: Разметить `src/options/index.html`**

Строка 2: `<html lang="ru">` → `<html lang="en">`.

Заголовок документа:

```html
    <title data-i18n="optionsTitle">KeenSwitch settings</title>
```

Шапка страницы:

```html
        <h1 data-i18n="optionsTitle">KeenSwitch settings</h1>
      </div>
      <p class="page-sub" data-i18n="optionsIntro">
        Two steps: connect to the router, then say which device to switch.
      </p>
```

Карточка роутера:

```html
            <h2 data-i18n="sectionRouterTitle">1. Router</h2>
            <p class="card-sub" data-i18n="sectionRouterSub">Sign-in details for the Keenetic web interface.</p>
```

```html
              <label for="base-url" data-i18n="fieldBaseUrl">Web interface address</label>
              <input id="base-url" type="text" spellcheck="false" placeholder="http://192.168.1.1" />
              <span class="hint" data-i18n="fieldBaseUrlHint">The same address you use to open the Keenetic panel.</span>
```

Атрибут `placeholder` поля адреса — это пример IP, а не текст: он не переводится и `data-i18n-placeholder` не получает.

```html
              <label for="login" data-i18n="fieldLogin">Username</label>
```

```html
              <label for="password" data-i18n="fieldPassword">Password</label>
```

Кнопка показа пароля:

```html
                <button
                  id="toggle-password"
                  class="icon-button"
                  type="button"
                  data-i18n-aria-label="passwordShow"
                  data-i18n-title="passwordShow"
                  aria-label="Show password"
                  aria-pressed="false"
                  title="Show password"
                ></button>
```

Предупреждение о пароле — проза после `<code>` выносится в собственный `<span>`:

```html
              <span>
                <strong data-i18n="passwordWarnTitle">The password is stored in plain text</strong>
                <code>chrome.storage.local</code>
                <span data-i18n="passwordWarnBody">is readable by anyone with access to your Chrome profile. The password is sent only to the address you specified, and only as a hash (SHA-256 of the challenge).</span>
              </span>
```

Кнопка проверки:

```html
            <button id="test" class="secondary" type="button" data-i18n="testConnection">Test connection</button>
```

Карточка устройства:

```html
            <h2 id="device-mode-title" data-i18n="sectionDeviceTitle">2. My device</h2>
            <p class="card-sub" data-i18n="sectionDeviceSub">This is the device whose policy will be switched.</p>
```

Режим «автоматически» — элемент `<code>whoami</code>` убирается, слово уезжает в текст сообщения:

```html
                <span class="choice-body">
                  <span class="choice-title" data-i18n="modeAutoTitle">Detect automatically</span>
                  <span class="hint" data-i18n="modeAutoHint">
                    The router reports the MAC address the request came from (RCI whoami). Works as
                    long as the browser reaches the router directly.
                  </span>
                </span>
```

Режим «вручную»:

```html
                <span class="choice-body">
                  <span class="choice-title" data-i18n="modeManualTitle">Pick manually from the list</span>
                  <span class="hint" data-i18n="modeManualHint">
                    Needed when auto-detection returns the wrong MAC address (VPN, bridges, docking
                    stations).
                  </span>
                </span>
```

Поле устройства:

```html
              <label for="device" data-i18n="fieldDevice">Device</label>
              <select id="device" disabled>
                <option value="" data-i18n="deviceListPlaceholder">— the list loads after you test the connection —</option>
              </select>
```

Кнопка сохранения:

```html
          <button id="save" class="primary" type="submit" data-i18n="save">Save</button>
```

- [ ] **Step 6: Перевести `src/options/options.ts` на ключи**

Добавить импорт после `import { icon, type IconName } from '../lib/icons';`:

```ts
import { localizeDocument, t } from '../lib/i18n';
```

`syncDeviceMode`:

```ts
  deviceHint.textContent =
    !modeAuto.checked && knownHosts.length === 0 ? t('deviceHintNeedTest') : '';
```

`renderHosts` — пустой список и пометки в вариантах:

```ts
  if (hosts.length === 0) {
    const option = document.createElement('option');
    option.value = '';
    option.textContent = t('deviceListEmpty');
    deviceSelect.append(option);
    syncDeviceMode();
    return;
  }

  for (const host of hosts) {
    const option = document.createElement('option');
    option.value = host.mac;
    const marks = [host.ip, host.mac];
    if (host.mac === whoamiMac) marks.push(t('markThisDevice'));
    else if (!host.active) marks.push(t('markOffline'));
    option.textContent = `${host.label} (${marks.filter(Boolean).join(', ')})`;
    deviceSelect.append(option);
  }
```

`refreshPermissionState`:

```ts
  if (!baseUrl) {
    setFeedback(permissionState, t('permNeedUrl'), 'muted');
    return;
  }
  const granted = await hasRouterPermission(baseUrl);
  setFeedback(
    permissionState,
    granted ? t('permGranted', baseUrl) : t('permNotGranted', baseUrl),
    granted ? 'ok' : 'muted',
  );
```

`syncPasswordToggle`:

```ts
  const label = t(visible ? 'passwordHide' : 'passwordShow');
```

Обработчик кнопки проверки:

```ts
  if (!baseUrl) {
    setFeedback(testResult, t('badUrl'), 'error');
    baseUrlInput.focus();
    return;
  }
  if (!passwordInput.value) {
    setFeedback(testResult, t('needPassword'), 'error');
    passwordInput.focus();
    return;
  }
```

```ts
  setPending(testResult, t('testing'));
```

```ts
      if (!granted) {
        setFeedback(testResult, t('permDenied'), 'error');
        return;
      }
```

```ts
      const whoamiHost = result.hosts.find((host) => host.mac === result.whoamiMac);
      setFeedback(
        testResult,
        result.whoamiMac
          ? t('testOkWhoami', [
              String(result.hosts.length),
              whoamiHost?.label ?? result.whoamiMac,
            ])
          : t('testOkNoWhoami', String(result.hosts.length)),
        'ok',
      );
```

Обработчик отправки формы:

```ts
  if (!settings.baseUrl) {
    setFeedback(saveResult, t('badUrl'), 'error');
    baseUrlInput.focus();
    return;
  }
  if (!settings.autoDetectDevice && !settings.deviceMac) {
    setFeedback(saveResult, t('needDevice'), 'error');
    deviceSelect.focus();
    return;
  }

  saveButton.disabled = true;
  setPending(saveResult, t('saving'));

  saveSettings(settings)
    .then(() => refreshPermissionState(settings.baseUrl))
    .then(() => setFeedback(saveResult, t('saved'), 'ok'))
```

Обработчик `change` на поле адреса:

```ts
  if (!normalized) {
    setFeedback(permissionState, t('badUrl'), 'error');
    return;
  }
```

Первая строка `init`:

```ts
async function init(): Promise<void> {
  localizeDocument();
  syncPasswordToggle(false);
```

- [ ] **Step 7: Убедиться, что русских строк в настройках не осталось**

Run: `grep -n "[А-Яа-яЁё]" src/options/options.ts src/options/index.html src/background.ts src/lib/*.ts`
Expected: только комментарии. Ни одного текстового литерала и ни одного текста в разметке.

- [ ] **Step 8: Проверить страницы в браузере**

Run: `npm run build`

Затем в Chrome: `chrome://extensions` → «Загрузить распакованное расширение» → папка `dist`. Открыть попап и страницу настроек — при английском интерфейсе Chrome весь текст английский. Переключить язык Chrome на русский (`chrome://settings/languages`, «Отображать Google Chrome на этом языке», перезапуск) — весь текст русский.

Expected: обе страницы полностью на одном языке, `__MSG_`-плейсхолдеров и ключей вида `errAuthRejected` в интерфейсе не видно.

- [ ] **Step 9: Полная проверка и коммит**

Run: `npm run typecheck && npm test && npm run build`
Expected: всё зелёное.

```bash
git add src/options public/_locales
git commit -m "feat(i18n): локализация страницы настроек

Проза после <code>chrome.storage.local</code> вынесена в отдельный span:
идентификатор стоит первым и во всех шести языках нормально становится
подлежащим.

Элемент <code>whoami</code> убран, слово ушло в текст сообщения: он стоял
в середине фразы, и разрез зафиксировал бы порядок слов для всех языков."
```

---

### Task 5: Локали uk, pl, de, tr

**Files:**
- Create: `public/_locales/uk/messages.json`
- Create: `public/_locales/pl/messages.json`
- Create: `public/_locales/de/messages.json`
- Create: `public/_locales/tr/messages.json`

**Interfaces:**
- Consumes: `public/_locales/en/messages.json` — полный каталог из 82 ключей после задач 1–4. Он источник правды и по набору ключей, и по смыслу.
- Produces: ничего для последующих задач.

К этому моменту расширение полностью работает на en и ru. Задача механическая: перевести английский каталог на четыре языка, ничего не добавляя и не выбрасывая.

**Правила перевода (обязательны, проверяются тестом или ревью):**

1. Набор ключей в каждом файле — **ровно** такой же, как в `en`. Ни одного лишнего, ни одного пропущенного.
2. Блоки `placeholders` копируются как есть: те же имена, тот же `content`. Имя плейсхолдера (`$status$`, `$policy$`, `$url$`, `$count$`, `$device$`, `$expected$`, `$actual$`, `$action$`, `$reason$`) внутри текста **не переводится** — переставлять его по фразе можно, переименовывать нельзя.
3. `appDesc` — не длиннее **132 символов**. Немецкий особенно склонен вылезать; при переполнении фразу сокращать, а не переносить.
4. Не переводятся: `KeenSwitch`, `Keenetic`, `KeeneticOS`, `Chrome`, `MAC`, `VPN`, `RCI`, `whoami`, `chrome.storage.local`, `declarativeNetRequest`, `SHA-256`, `challenge`, `Origin`, `cookie`, `/auth`, `/rci/`, `chrome://extensions`, `HTTP`.
5. Счётные строки (`testOkWhoami`, `testOkNoWhoami`) остаются в форме «Подпись: N» с двоеточием. Не переписывать их во фразу вида «Найдено 5 устройств» — в польском и украинском это потребует трёх форм числа, которых в `chrome.i18n` нет.
6. Кавычки — по типографике языка: `uk` — «…», `pl` — „…”, `de` — „…“, `tr` — “…”. Затронуты ключи `statusSwitching`, `statusSwitched`, `errPolicyNotAppliedDetail`, `deviceHintNeedTest`, `permNotGranted`.
7. `passwordWarnBody` начинается со сказуемого: в разметке перед ним стоит `<code>chrome.storage.local</code>`, и фраза должна читаться как продолжение с этим подлежащим.
8. Многоточие — символом `…`, а не тремя точками: так в `en` и `ru`.
9. Обращение к пользователю — вежливое и нейтральное: `de` — «Sie», `pl` — безличные формы или 2 л. мн. ч., `tr` — форма на «-iniz».

**Образец формата** — как выглядят четыре характерных ключа во всех четырёх локалях (скопировать структуру, остальные 78 ключей переводятся так же):

`de`:

```json
  "appDesc": {
    "message": "Wechselt dieses Gerät mit einem Klick zwischen den Zugriffsrichtlinien des Keenetic-Routers."
  },
  "statusSwitching": {
    "message": "Wechsle zu „$policy$“…",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "testOkWhoami": {
    "message": "Verbindung steht. Geräte: $count$. Dieses Gerät: $device$.",
    "placeholders": { "count": { "content": "$1" }, "device": { "content": "$2" } }
  },
  "passwordWarnBody": {
    "message": "ist für jeden lesbar, der Zugriff auf Ihr Chrome-Profil hat. Das Passwort geht nur an die angegebene Adresse und nur als Hash (SHA-256 des Challenge)."
  }
```

`uk`:

```json
  "statusSwitching": {
    "message": "Перемикаю на «$policy$»…",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "testOkWhoami": {
    "message": "Зв'язок є. Пристроїв: $count$. Цей пристрій: $device$.",
    "placeholders": { "count": { "content": "$1" }, "device": { "content": "$2" } }
  }
```

`pl`:

```json
  "statusSwitching": {
    "message": "Przełączam na „$policy$”…",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "testOkWhoami": {
    "message": "Połączono. Urządzenia: $count$. To urządzenie: $device$.",
    "placeholders": { "count": { "content": "$1" }, "device": { "content": "$2" } }
  }
```

`tr`:

```json
  "statusSwitching": {
    "message": "“$policy$” politikasına geçiliyor…",
    "placeholders": { "policy": { "content": "$1" } }
  },
  "testOkWhoami": {
    "message": "Bağlantı kuruldu. Cihazlar: $count$. Bu cihaz: $device$.",
    "placeholders": { "count": { "content": "$1" }, "device": { "content": "$2" } }
  }
```

- [ ] **Step 1: Создать `public/_locales/de/messages.json`**

Перевести все 82 ключа из `public/_locales/en/messages.json` на немецкий по правилам выше.

- [ ] **Step 2: Проверить каталог `de`**

Run: `npm test`
Expected: PASS. Если набор ключей или плейсхолдеры разошлись с `en`, тест назовёт локаль и ключ. Если `appDesc` длиннее 132 символов — тест назовёт фактическую длину.

- [ ] **Step 3: Создать `public/_locales/uk/messages.json`**

Перевести все 82 ключа на украинский по правилам выше.

- [ ] **Step 4: Проверить каталог `uk`**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Создать `public/_locales/pl/messages.json`**

Перевести все 82 ключа на польский по правилам выше.

- [ ] **Step 6: Проверить каталог `pl`**

Run: `npm test`
Expected: PASS.

- [ ] **Step 7: Создать `public/_locales/tr/messages.json`**

Перевести все 82 ключа на турецкий по правилам выше.

- [ ] **Step 8: Проверить каталог `tr`**

Run: `npm test`
Expected: PASS.

- [ ] **Step 9: Проверить сборку и упаковку на шести локалях**

Run: `npm run typecheck && npm test && npm run build && npm run package`
Expected: в листинге архива шесть каталогов `_locales/*/messages.json`, в конце — `локалей: 6 (de, en, pl, ru, tr, uk)`.

- [ ] **Step 10: Проверить одну неанглийскую локаль в браузере**

Загрузить `dist` в `chrome://extensions`, переключить язык интерфейса Chrome на немецкий и открыть попап и настройки.

Expected: интерфейс по-немецки, текст не вылезает за границы кнопок и плашек. Немецкие строки заметно длиннее английских — если что-то обрезается, проблема в вёрстке `src/ui.css`, а не в переводе; починить надо её.

- [ ] **Step 11: Коммит**

```bash
git add public/_locales
git commit -m "feat(i18n): локали uk, pl, de, tr

Переводы uk, pl и tr — черновые и требуют вычитки носителями до релиза:
машинный перевод в сообщениях об ошибках сетевого оборудования легко даёт
формулировки, которые выглядят неловко."
```

---

## Проверка плана на соответствие спеке

| Требование спеки | Где выполняется |
| --- | --- |
| Локали en, ru, uk, pl, de, tr | Задачи 1 (en, ru), 5 (uk, pl, de, tr) |
| `default_locale: 'en'` | Задача 1, шаг 7 |
| Каталоги в `public/_locales`, Vite копирует без доп. настройки | Задача 1, шаги 5 и 8 |
| `name` остаётся литералом, `__MSG_` только у `description` | Задача 1, шаг 7 |
| `t()` возвращает ключ без глобального `chrome` | Задача 1, шаги 2 и 4 |
| `applyI18n` по `data-i18n` и трём атрибутам | Задача 1, шаг 4; применение — задачи 3 и 4 |
| `document.documentElement.lang` в рантайме | Задача 1, шаг 4 (`localizeDocument`) |
| `KeeneticError` на `key`/`detailKey` | Задача 2, шаг 3 |
| `assertNoRciError` принимает ключ действия | Задача 2, шаги 1 и 4 |
| Сырой текст роутера не переводится | Задача 2, шаг 3 (`detailText`), тест в шаге 1 |
| Плюрали обходятся формой «Подпись: N» | Задача 4 (ключи `testOkWhoami`, `testOkNoWhoami`), задача 5, правило 5 |
| Проверка 132 символов по каждой локали | Задача 1, шаг 9 (`package.mjs`) и шаг 2 (тест) |
| Тест целостности каталогов | Задача 1, шаг 2 |
| `tests/auth.test.ts` на `error.key` | Задача 2, шаг 1 |
| Инвентарь 82 ключа | 1 + 28 + 15 + 38 = 82 |
| README и карточка Store вне объёма | Не входят ни в одну задачу |
